<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Module Category -->
        <record id="module_category_overdraft" model="ir.module.category">
            <field name="name">Loan Management</field>
            <field name="sequence">50</field>
        </record>

        <!-- Viewer Group -->
        <record id="group_overdraft_viewer" model="res.groups">
            <field name="name">Viewer</field>
            <field name="category_id" ref="module_category_overdraft"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        </record>

        <!-- Editor Group (inherits Viewer) -->
        <record id="group_overdraft_editor" model="res.groups">
            <field name="name">Editor</field>
            <field name="category_id" ref="module_category_overdraft"/>
            <field name="implied_ids" eval="[(4, ref('group_overdraft_viewer'))]"/>
            <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
        </record>
    </data>
</odoo>
